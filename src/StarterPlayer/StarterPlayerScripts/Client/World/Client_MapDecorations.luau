-- Map decorations are placed on the server; no client spawn animation during reconstruction.
local Client_MapDecorations = {}

function Client_MapDecorations.Init() end

return Client_MapDecorations
